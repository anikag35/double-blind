[project]
name = "double-blind"
version = "0.1.0"
description = "Double Blind: a distributed, fault-tolerant LLM evaluation platform"
requires-python = ">=3.11"
dependencies = [
    "grpcio>=1.60",
    "grpcio-tools>=1.60",
]

[tool.setuptools]
packages = ["worker"]